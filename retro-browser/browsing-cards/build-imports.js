function import_me(link_load) {	var link = link_load.currentTarget;	var content = link.import;	var all_content = content.querySelectorAll('*');	var conlen = all_content.length;	for(var i = 0; i < conlen; i++) {		function do_append_part(nodei) {			return function () {				document.body.appendChild(nodei.cloneNode(true));			};		}		setTimeout(do_append_part(all_content[i]), 0);	}		}function activate_imports() {	var imports = document.querySelectorAll('link[rel=import]');	var importlen = imports.length;	for(var i = 0; i < importlen; i++) {		var importi = imports[i];		var src = importi.dataset.href;		bind(importi,'load',import_me);			if(!!src) {			importi.setAttribute('href',src);		}	}	}activate_imports();
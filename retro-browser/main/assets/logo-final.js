/* logo js */		var deg1 = 3.14159*2/360;		var step = deg1*15;		var cx = window.innerWidth/2*0.3;		var cy = window.innerHeight/2*0.3;		console.log(cx,cy);		var radiusa,radiusb;		var limita,limitb;		limita = 700*deg1;		limitb = 518*deg1;		var frequency = 40;		function do_move_elem_through_circle(elem,deg,radius,limit) {			return function () {				deg = deg + step;				var x = radius*Math.cos(deg);				var y = radius*Math.sin(deg);					elem.style.top = y+"px";				elem.style.left = x+"px";					/*console.log(x,y,cx,cy,radius,deg);*/				if(deg < limit) {					setTimeout(do_move_elem_through_circle(elem,deg,radius,limit),1.0/frequency*1000);				}			};		}		function move_clouds() {			radiusa = parseInt(getComputedStyle(document.querySelector('.fa-circle')).width)*0.37;			radiusb = parseInt(getComputedStyle(document.querySelector('.fa-circle')).width)*0.33;			console.log(radiusa,radiusb);			setTimeout(do_move_elem_through_circle(document.querySelector('.clouda'),0,radiusa,limita),1.0/frequency*1000);			setTimeout(do_move_elem_through_circle(document.querySelector('.cloudb'),-220*deg1,radiusb,limitb),1.0/frequency*1000);		}		on_start_do(move_clouds);
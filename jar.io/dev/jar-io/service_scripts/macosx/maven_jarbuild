jarname=$1
guid=$2
project=$3
entrypoint=$4
staging=~/.jar.io/tmp/build_staging/$guid/$project
build=$staging/target

# clean

echo "Cleaning build directory..."
cd $staging
mvn clean

# building with maven

echo "Building with maven..."
mvn package

echo "Done."

